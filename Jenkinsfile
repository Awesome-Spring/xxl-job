@Library('global-shared-library') _
/*
生成pipeline模版
generatePipelineTemplate (
            List<String> choices,// 需要构建模块
            boolean isMutiModules,// 是否maven多模块项目
            List<String> codeAnalysisModules,// 需要做静态代码质量分析的模块
            boolean abortPipeline,// sonarqube质量分析不通过是否中止pipeline
            Map pushImageConfigMap,// 扩展使用，暂时填空map或null
            Map deployToDevConfigMap // 扩展使用，暂时填空map或nul
            )
*/
generatePipelineTemplate(
    ['xxl-job-admin'],
    true,
    [],
    false,
    [:],
    [:]
   )
